language: generic

sudo: required
services:
  - docker
  -
before_install:
  - docker build -t billgonz/docker-react -f Dockerfile.dev .


    script:
      - docker run -e CI=true billgonz/docker-react npm run test -- --coverage


